Tujuan: Melatih string immutability, rune vs byte, UTF-8, strconv, fmt.

Fitur:

User input sebuah kalimat → tampilkan:

Panjang string (byte vs rune)

Huruf pertama & terakhir dalam bentuk:

byte

rune

ASCII atau UTF-8?

Reverse string versi UTF-8 (harus menggunakan rune).

Bagus banget untuk memahami rune/byte & encoding.